diff --git a/rtl/hazard_unit.sv b/rtl/hazard_unit.sv
--- a/rtl/hazard_unit.sv
+++ b/rtl/hazard_unit.sv
@@ -11,6 +11,5 @@ module hazard_unit (
         stall_o = 1'b0;
         if (id_ex_mem_read_i && (id_ex_rd_i != 5'd0)) begin
-            stall_o = (if_id_uses_rs1_i && (id_ex_rd_i == if_id_rs1_i)) ||
-                      (if_id_uses_rs2_i && (id_ex_rd_i == if_id_rs2_i));
+            stall_o = (if_id_uses_rs1_i && (id_ex_rd_i == if_id_rs1_i));
         end
     end
 endmodule
